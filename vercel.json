{
    "version": 2,
    "builds": [
      {
        "src": "servidor.js",
        "use": "@vercel/node",
        "config": { "includeFiles": ["public/**", "build/**"] }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "servidor.js" }
    ]
  }
  